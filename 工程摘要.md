- 运行板工程：[lq_2k0301_project_template_for_running](/d:/aaa走马观碑代码/双板代码/lq_2k0301_project_template_for_running)
- 识别板工程：[lq_2k0301_project_template_for_recognition](/d:/aaa走马观碑代码/双板代码/lq_2k0301_project_template_for_recognition)

下文中统一约定：
- `running` = `lq_2k0301_project_template_for_running`
- `recognition` = `lq_2k0301_project_template_for_recognition`

**摘要使用规则**
- 本摘要以当前源码为准，优先级高于两个历史 README：
  - `running/Code/User/image/README_IMAGE_PIPELINE.md`
  - `recognition/README_IMAGE_PIPELINE.md`
- 修改业务逻辑优先看 `Code/**` 与两个 `common.h`，不要先改这些目录：
  - `build/`
  - `output/`
  - `cross_lib/`
  - `model_backup_*`
- 参数入口优先级：
  - 运行板图像链参数：`running/Code/User/image/common.h`
  - 识别板参数：`recognition/Code/User/inc/common.h`
  - 运行板控制/发车/直道加速部分参数：`running/Code/User/src/PID.cc`（尚未完全收口到 `common.h`）

**用户偏好与工作风格**
- 高风险或语义不清的地方要先问。
- 用户非常重视“结构清晰、长期可维护”，不喜欢表面式重命名。
- 用户喜欢：
  - 调参宏集中到 `common.h`
  - 宏按 `#pragma region ... #pragma endregion` 分组
  - 关键链路有特殊注释和“为什么这样设计”的注释
  - 开关尽量放文件顶部或公共配置入口
- 用户经常要求：
  - 先梳理链路
  - 再收敛状态 owner
  - 再提公共逻辑
  - 再集中参数
- 用户明确要求过：对于不确定的旧逻辑，不要臆测重写，要保守并标风险。

**双板边界**
- 运行板：灰度巡线、元素状态机、`pure_angle`、控制闭环、VOFA、菜单、串口接收。
- 识别板：`640x480` 彩色采集、红块 ROI 几何、ONNX 分类、状态流发送、图传。
- 当前源码下的固定语义：
  - `weapon -> w -> 运行板左侧锁边 + 短时激进 pure_angle 接管`
  - `supply -> s -> 运行板右侧锁边 + 短时激进 pure_angle 接管`
  - `vehicle -> v -> 运行板保持收到包当下的 pure_angle`
  - `no_result -> u -> 运行板保持 pure_angle 窗口，并把基础速度下调`
  - `brick -> b -> 运行板在特定入环阶段压制环岛状态机，并回到 MIXED`
  - `unknown -> n -> 清空远端覆盖，回到本地巡线`

**目录与构建**

| 位置 | 作用 | 修改注意 |
| --- | --- | --- |
| `running/CMakeLists.txt` | 运行板构建 | `file(GLOB_RECURSE ...)` 会把 `Code/**` 与 `LQ_Libraries_*` 下新增源文件自动编进来；默认不再构建旧单板 `ncnn` 链，`BW_RUNNING_ENABLE_LEGACY_NCNN=ON` 才会重新启用；OpenCV 默认优先走 `cross_lib/opencv/lib/cmake/opencv4`。 |
| `recognition/CMakeLists.txt` | 识别板构建 | 只显式编译 `main.cpp`、`Communication.cc`、`recognition_runtime.cc`、`recognition_chain.cc`、`roi_runtime_geometry.cc`、`stream_chain.cc`、`transform_table.cc`；当前优先使用 `recognition/libraries/drv/src/liblq_camera_ex.a`，缺少时才回退到 `Code/User/src/lq_camera_ex_compat.cc`；新增识别板源码必须手动加到 `RECOGNITION_BOARD_SRC`；交叉编译器路径 `CROSS_COMPILER_DIR` 必须在这里改。 |
| `recognition/model/` | 识别板运行时模型资产 | 当前源码运行时会读取 `cls.onnx`、`class_names.json`、`deploy_calibration.json`；CMake 只 copy 了前两者，若改校准部署要同时看 `recognition/CMakeLists.txt` 和 `recognition/Code/User/src/recognition_chain.cc`。 |
| `running/Code/main.hpp` | 运行板“总包含头” | 这里聚合旧库、新库、用户头文件和 `extern` 全局对象声明，本身不是主要业务 owner。 |
| `recognition/Code/main.hpp` | 识别板“总包含头” | 这里只维护识别板相机、图传、串口相关最小依赖。 |

**入口与线程**

| 板子 | 入口 | 实际执行顺序 | 当前活跃线程/循环 |
| --- | --- | --- | --- |
| 运行板 | `running/Code/main.cpp` | `system_init()` -> 启动定时线程 -> 非阻塞终端 -> 解析 `--stream` -> 进入 `Vision_System_Run(stream_enabled)` | `BayWatcher_Control_Loop`(5ms)、`BayWatcher_Cube_Loop`(10ms)、`task_sensor_read`(20ms)、`task_vofa_comm`(20ms)、`task_board_comm_rx`(5ms)、`task_display_status`(5000ms)、`Vision_System_Run` 阻塞主循环 |
| 识别板 | `recognition/Code/main.cpp` | `system_init()` -> 非阻塞终端 -> 解析 `--stream` / `--recognition` -> 进入 `RunRecognitionBoard(...)` | 单线程阻塞主循环；循环内完成采集、ROI、分类、发包、图传 |

**运行板当前真实入口**
- `running/Code/main.cpp`
  - 定义运行板大多数全局对象：`PID`、`ESC`、`encoder_sys`、`motor_sys`、`camera`、`server`、`img/binimg/view`、`handler_sys` 等。
  - `system_init()` 当前会初始化灰度相机 `160x120@vision_runtime::kLineFrameFps`、串口 `comm.init(UART1, B115200)`、IMU、编码器、电机、电调、菜单等。
  - `task_board_comm_rx()` 是运行板唯一的双板收包入口，收到新包后调用 `image_remote_recognition_apply_state(code, seq, pure_angle, now_ms)`。
- `running/Code/User/image/vision_runtime.h/.cc`
  - `Vision_System_Run(bool stream_enabled)` 是当前运行板视觉顶层唯一入口。
  - 主循环顺序：
    1. 读键盘 `c/C`
    2. 灰度采集 `camera.capture_frame(..., cv::IMREAD_GRAYSCALE, ...)`
    3. `handler_sys.Update_Tick()`
    4. 若 `handler_sys.TryGetPureAngleOverride()` 命中，则直接走纯角度接管分支
    5. 否则做正常二值化 + `img_processing(binimg)`
    6. 可选图传 `stream.PublishFrame(view)`

**识别板当前真实入口**
- `recognition/Code/main.cpp`
  - 定义识别板全局对象：`std::unique_ptr<lq_camera_ex> camera`、`server`、`img`、`view`。
  - `system_init()` 当前会：
    - 固定申请 `640x480@BW_RECOG_CAMERA_FPS`
    - 优先按 `BW_RECOG_CAMERA_TRY_0CPU_MJPG` 决定是否尝试低 CPU MJPG
    - 识别板串口通过 `init_board_comm_with_retry()` 初始化并有限重试
  - 运行时参数：
    - 图传：`--stream` / `--no-stream` / `--stream=on|off`
    - 识别：`--recognition` / `--no-recognition` / `--recognition=on|off`
- `recognition/Code/User/inc/recognition_runtime.h`
  - `RunRecognitionBoard(bool stream_enabled, bool recognition_enabled_by_switch)` 是识别板主循环唯一入口。

**核心对象与状态 owner**

| 对象/状态 | 定义位置 | 主要写入者 | 主要读取者 |
| --- | --- | --- | --- |
| `BoardComm comm` | 两板各自的 `Code/User/src/Communication.cc` | `init/send_state/try_receive_state` | 运行板 `task_board_comm_rx()`；识别板 `RunRecognitionBoard()` |
| 运行板全局对象 `encoder_sys`、`motor_sys`、`esc_sys`、`camera`、`server`、`img/binimg/view`、`handler_sys`、`PID` 等 | `running/Code/main.cpp` | 各自模块实现文件 | `main.hpp` 引入后的全工程 |
| 识别板全局对象 `camera`、`server`、`img`、`view` | `recognition/Code/main.cpp` | `main.cpp`、`recognition_runtime.cc` | 识别板运行时与图传链 |
| 图像侧全局状态 `pts_left/right`、`pts_far_left/right`、`midline`、`pure_angle`、`follow_mode`、`element_type`、`circle_state`、`crossing_state`、`zebra_stop` | `running/Code/User/image/image_data.cc` | `image_process.cc`、`image_handle.cc`、`element*.cc` | `vision_runtime.cc`、`PID.cc`、VOFA、元素状态机 |
| 远端识别运行态 `g_remote_recognition` | `running/Code/User/image/image_data.cc` | `image_remote_recognition_apply_state()` | `img_processing()` 通过 `image_remote_recognition_*()` 查询 |
| 控制侧全局状态 `PID` / `ESC` / `PID_Speed_L/R` / `PID_Angle` / `PID_Cube` | `running/Code/main.cpp`，结构定义在 `running/Code/User/inc/PID.h` | `PID.cc`、菜单、VOFA | 电机、电调、菜单、VOFA |
| 识别板内部状态 `enabled_`、`mode_`、`prob_sum_`、`valid_frame_count_`、`current_vision_code_`、`latched_symbol_code_`、`current_blob_area_` | `recognition/Code/User/inc/recognition_chain.h` 的 `RecognitionChain` 私有成员 | `recognition_chain.cc` | `recognition_runtime.cc` 通过 getter 查询 |

**关键数据结构**

`BoardVisionCode` / `BoardStatePacket`
- 位置：两板 `Code/User/inc/Communication.h`
- 状态码：
  - `VEHICLE = 'v'`
  - `WEAPON = 'w'`
  - `SUPPLY = 's'`
  - `BRICK = 'b'`
  - `NO_RESULT = 'u'`
  - `UNKNOWN = 'n'`
- 包格式固定为 7 字节：
  - `header1`
  - `header2`
  - `version`
  - `seq`
  - `code`
  - `crc8`
  - `tail`

`pts_well_processed`
- 位置：`running/Code/User/image/image_data.h`
- 作用：单侧边线从“原始点 -> 逆透视 -> 滤波 -> 重采样 -> 曲率 -> 角点 -> 单边候选中线”的完整上下文。
- 当前字段重点：
  - `pts` / `pts_count`
  - `pts_inv`
  - `pts_filter`
  - `pts_resample`
  - `resample_src_id`
  - `curvature` / `curvature_nms`
  - `corner_found` / `corner_id` / `corner`
  - `mid` / `mid_count`

`midline_data`
- 位置：`running/Code/User/image/image_data.h`
- 作用：
  - `mid`：最终中线
  - `path`：从 `core` 并轨到中线后的控制路径

`Bay_PID_t`
- 位置：`running/Code/User/inc/PID.h`
- 当前活跃字段重点：
  - 运行状态：`is_running`
  - 发车状态机：`startup_state`、`startup_delay_cnt`、`startup_speed_target`、`startup_esc_target`
  - 控制输入：`base_target_speed`、`target_angle`
  - 视觉输入：`vision_yaw`
  - 控制输出：`speed_adjust`、`target_speed_L/R`、`pwm_out_L/R`

`TargetBoardType` / `ActionStage` / `BayWatcher_TargetHandler`
- 位置：`running/Code/User/inc/TargetHandler.h`
- 这是“纯 `pure_angle` 接管式绕行状态机”的单独封装。
- 重要事实：当前串口接收到的 `w/s` 并不会自动进入 `Start_Action()`，所以它不是当前双板主路径 owner，只是预留/可切换方案。

`RecognitionChain`
- 位置：`recognition/Code/User/inc/recognition_chain.h`
- 当前关键成员：
  - 总开关：`enabled_`
  - 模式：`mode_` (`NORMAL` / `RECOGNITION`)
  - 概率累计：`prob_sum_`
  - 分类后校准：`logit_bias_`、`calibration_temperature_`
  - 收敛计数：`valid_frame_count_`
  - 收敛门限：`min_valid_frames_`、`max_valid_frames_`、`decision_top1_threshold_`、`decision_margin_threshold_`
  - 当前输出状态：`current_vision_code_`
  - 成功结果保持：`latched_symbol_code_`、`latched_release_pending_`、`latched_release_deadline_ms_`
  - 观测量：`current_blob_area_`
  - 红块近期可见窗口：`recent_red_candidate_until_ms_`
  - 单帧性能样本：`last_perf_sample_`

`RoiExtractionResult`
- 位置：`recognition/Code/User/inc/roi_runtime_geometry.h`
- 作用：描述一次 ROI 几何提取的全部中间结果与 reject 原因。
- 当前重点字段：
  - `status`
  - `target_type`
  - `search_rect`
  - `blob_box` / `loose_blob_box`
  - `candidate_area`
  - `roi_quad`
  - `roi_bgr`
  - `reference_x_min/max`
  - `touches_search_top` / `touch_top_expand` / `touch_top_brick_override`
  - `ipm_reason`

`RoiQualityMetrics`
- 位置：`recognition/Code/User/inc/roi_runtime_geometry.h`
- 作用：ROI 低信息过滤指标。
- 关键字段：`gray_std_top`、`canny_density_top`、`lap_var_top`、`valid`、`reason`

**运行板数据流**

1. 灰度采集
- 文件：`running/Code/User/image/vision_runtime.cc`
- 相机对象：`WW_CAMERA` 封装的 `camera`
- 固定工作分辨率：`160x120`

2. 二值化与预处理
- 文件：`running/Code/User/image/vision_runtime.cc`
- 顺序：
  - Otsu 二值化
  - 开运算
  - 闭运算
  - 四边清零
  - `img_processing(binimg)`

3. 边线搜索
- 文件：
  - `running/Code/User/image/image_process.cc`
  - `running/Code/User/image/image_handle.cc`
- 当前真实调用链：
  - `process_track_edges()`
  - `SearchLine_Lpt()` / `SearchLine_Rpt()`
  - `process_line(true, pts_left)` / `process_line(false, pts_right)`

4. 元素状态机
- 文件：
  - `running/Code/User/image/element.cc`
  - `running/Code/User/image/element/circle.cc`
  - `running/Code/User/image/element/crossing.cc`
  - `running/Code/User/image/element/zebra.cc`
- 当前主链：
  - `element_detect()`
  - 若 `CIRCLE`：`roundabout_update()`
  - 若 `CROSSING`：`crossing_update()` + `crossing_far_line_check(img)`

5. 中线与路径
- 文件：
  - `running/Code/User/image/image_process.cc`
  - `running/Code/User/image/image_midline_process.cc`
- 当前真实路径：
  - `build_midline_from_current_state()` -> `MID(...)`
  - `BuildPathFromCoreToMidlineArc(...)`
  - `CalculatePureAngleFromPath(...)`

6. 控制闭环
- 文件：`running/Code/User/src/PID.cc`
- 当前活跃线程：
  - `BayWatcher_Cube_Loop()`：`pure_angle -> PID.speed_adjust`
  - `BayWatcher_Control_Loop()`：编码器反馈 + `PID.speed_adjust` -> `PID.target_speed_L/R` -> `PID.pwm_out_L/R`
- 当前电机执行：
  - `motor_sys.Motor2_Set(PID.pwm_out_L)`
  - `motor_sys.Motor1_Set(PID.pwm_out_R)`

**运行板图像侧公共接口**

| 接口 | 位置 | 作用 |
| --- | --- | --- |
| `Vision_System_Run(bool)` | `running/Code/User/image/vision_runtime.h/.cc` | 视觉总入口 |
| `img_processing(const cv::Mat&)` | `running/Code/User/image/image_process.h/.cc` | 二值图总控入口 |
| `MID(...)` | `running/Code/User/image/image_midline_process.h/.cc` | 左右候选中线融合 |
| `BuildPathFromCoreToMidlineArc(...)` | `running/Code/User/image/image_midline_process.h/.cc` | 从车体 `core` 并轨到中线 |
| `CalculatePureAngleFromPath(...)` | `running/Code/User/image/image_handle.h/.cc` | 路径转 `pure_angle` |
| `track_force_reset()` | `running/Code/User/image/element.h/.cc` | 强制清空元素状态机 |
| `image_remote_recognition_*()` | `running/Code/User/image/image_data.h/.cc` | 双板识别状态适配层 |

**识别板数据流**

1. 彩色采集
- 文件：`recognition/Code/main.cpp`
- 相机对象：`lq_camera_ex`
- 构建注意：当前标准库路径是 `recognition/libraries/drv/inc/lq_camera_ex.hpp` + `recognition/libraries/drv/src/liblq_camera_ex.a`；仅当静态库缺失时才回退到 `Code/User/src/lq_camera_ex_compat.cc`
- 固定工作分辨率：`640x480`

2. 运行时总调度
- 文件：`recognition/Code/User/src/recognition_runtime.cc`
- 每帧顺序：
  1. 可选 `c/C` 手动启动一轮检测
  2. 取当前彩色帧 `img = camera->get_frame_raw()`
  3. 若识别链禁用，则只出 disabled 画面
  4. 若 `RecognitionChain::IsInRecognitionMode()`，进入 `ProcessRecognitionFrame(...)`
  5. 否则走 `TryEnterRecognition(...)`
  6. 根据 `current_vision_code_` 决定是否发状态包
  7. 可选图传

3. 普通态与进入识别态
- 文件：`recognition/Code/User/src/recognition_chain.cc`
- 当前真实链路：
  - `ExtractRotatedRoi(...)`
  - `ExtractRotatedRoi(...)` 内部固定执行文档化红带流程：
    - 搜索带 `y=120..320`
    - `y=320` 白红带参考包络 + 搜索带严格红带包络
    - 严格红带掩码
    - `8px` 逐边外扩，最多 `12` 次
    - 连通域选最靠下红带
    - 面积阈值分 `marker/roadblock`
    - `marker` 走 `minAreaRect + IPM top-edge square ROI`
  - 若提到 `marker ROI` 则 `mode_ = RECOGNITION`
  - 若提到 `roadblock` 或无有效目标，则根据结果输出 `b/u/n`

4. 识别态
- 文件：`recognition/Code/User/src/recognition_chain.cc`
- 当前真实链路：
  - `ExtractRotatedRoi(...)`
  - `ComputeLowInformationRoiMetrics(...)` 仅做调试显示，不阻断 active flow
  - `classify_roi_index(...)`
  - 概率累计到 `prob_sum_`
  - 达到 `min_valid_frames_` 且阈值/间隔满足时输出 `w/s/v`
  - 否则输出 `u`

5. 成功结果保持与释放
- 文件：`recognition/Code/User/src/recognition_chain.cc`
- 当前逻辑：
  - 成功识别 `w/s/v` 后，把结果写入 `latched_symbol_code_`
  - 只要 marker 红块还可见，就维持该状态
  - 红块丢失后，按 `BW_RECOG_SIGN_LOSS_HOLD_MS` 保持
  - 超时后自动释放为 `n`

**识别板公共接口**

| 接口 | 位置 | 作用 |
| --- | --- | --- |
| `RunRecognitionBoard(bool, bool)` | `recognition/Code/User/inc/recognition_runtime.h` | 识别板总入口 |
| `RecognitionChain::Initialize()` | `recognition/Code/User/inc/recognition_chain.h/.cc` | 加载 ONNX、类别、校准，决定识别链是否上线 |
| `RecognitionChain::Reset()` | 同上 | 清空识别内部状态 |
| `RecognitionChain::TryEnterRecognition()` | 同上 | 普通态红块触发 |
| `RecognitionChain::ProcessRecognitionFrame()` | 同上 | 识别态 ROI 分类与概率收敛 |
| `ExtractRotatedRoi()` | `recognition/Code/User/inc/roi_runtime_geometry.h/.cc` | 红块几何到 `64x64 ROI` |
| `ComputeLowInformationRoiMetrics()` | 同上 | ROI 低信息调试指标；当前不阻断 active flow |

**双板协议与状态交互**

当前协议 owner：
- 运行板：`running/Code/User/inc/Communication.h/.cc`
- 识别板：`recognition/Code/User/inc/Communication.h/.cc`

当前发送策略：
- 识别板运行时文件：`recognition/Code/User/src/recognition_runtime.cc`
- 行为不是“只发一次性事件包”，而是“状态变化立即发包 + 状态不变时按心跳周期补发同状态”。
- `tx_seq` 只在状态变化时递增；心跳补发复用同一个 `seq`。

当前接收策略：
- 运行板接收线程：`running/Code/main.cpp / task_board_comm_rx()`
- `BoardComm::try_receive_state()` 负责从字节流重组 `BoardStatePacket`
- 运行板适配层：`image_remote_recognition_apply_state(code, seq, pure_angle, t_ms)`
- 适配层会用 `seq` 去重；同一个 `seq` 的心跳包仍会刷新 `last_rx_ms`，但不会重复触发动作

当前状态码在运行板的真实语义：
- `n`
  - 清空 `follow_state`
  - 清空 `vehicle_hold_*`
  - 清空 `aggressive_turn_*`
  - 清空 `hold_u_slowdown_until_aggressive_end`
  - 解除 `block_circle_until_n`
  - `follow_mode` 回到 `MIXED`
- `b`
  - 若当前 `circle_state` 处于 `CIRCLE_BEGIN` 或 `CIRCLE_IN`，则置 `block_circle_until_n = true`
  - 不会直接启动绕行动作
- `w`
  - 置 `follow_state = LEFT_EDGE_ROUTE`
  - 置 `aggressive_turn_state = FORCE_LEFT_30`
  - 激进接管最长持续 `BW_REMOTE_SIGN_AGGRESSIVE_MAX_MS`
  - 后续 `img_processing()` 会强制 `FollowLine::MIDLEFT`
- `s`
  - 置 `follow_state = RIGHT_EDGE_ROUTE`
  - 置 `aggressive_turn_state = FORCE_RIGHT_30`
  - 激进接管最长持续 `BW_REMOTE_SIGN_AGGRESSIVE_MAX_MS`
  - 后续 `img_processing()` 会强制 `FollowLine::MIDRIGHT`
- `v`
  - 置 `follow_state = VEHICLE_ROUTE`
  - 清空 `aggressive_turn_*`
  - 锁存“收到包当下”的 `pure_angle`
  - 保持时长：`BW_REMOTE_VEHICLE_HOLD_MS`
- `u`
  - 共用 `VEHICLE_ROUTE` 锁存当前 `pure_angle`
  - 基础速度比例改为 `BW_REMOTE_U_SLOWDOWN_RATIO`
  - 若先前仍在激进接管窗口，减速会保持到激进窗口结束

**运行板图像链里远端状态的介入点**

文件：`running/Code/User/image/image_process.cc`

当前真实逻辑：
1. 先执行 `image_remote_recognition_tick(t_ms)`，处理 stale 超时和激进接管到期
2. `vehicle_active = image_remote_recognition_is_vehicle_active(t_ms)`
3. `remote_follow_locked = image_remote_recognition_get_forced_follow_mode(...)`
4. `remote_circle_block = image_remote_recognition_should_block_circle()`
5. 若 `vehicle_active` 且当前画面无法可靠起线：
   - `process_track_edges()` 返回 `TRACK_SEARCH_VEHICLE_FALLBACK_HOLD`
   - 直接输出锁存的 `hold_yaw`
6. `vehicle` 特殊起线当前使用 `BW_REMOTE_VEHICLE_SKIP_HALF_WIDTH` 的中心窗口跳黑块逻辑
7. 若 `remote_route_active`：
   - 临时屏蔽元素状态机
   - `w/s` 用 `build_midline_from_remote_follow_override()` 直接选左/右单边候选中线
8. 若 `remote_circle_block`：
   - 清空环岛运行态
   - `follow_mode` 回到 `MIXED`
8.5 若命中斑马线冲线锁定：
   - 清空本地元素状态与远端识别状态
   - 锁定 `follow_mode = MIXED`
   - 屏蔽环岛、十字、远端 `w/s/u/v` 介入
   - 控制侧基础速度乘上 `BW_ZEBRA_RUSH_SPEED_RATIO`
9. `pure_angle` 在几何链、丢线补偿、趋势前馈之后，还会再过一层 `image_remote_recognition_get_aggressive_turn_override(...)`

**当前接口与交互细节**

运行板串口接口
- `BoardComm::init(const std::string& port = UART1, uint32_t baud = B115200)`
- `BoardComm::try_receive_state(BoardVisionCode* out_code, uint8_t* out_seq)`
- `BoardComm::send_state(BoardVisionCode code, uint8_t seq)`

识别板串口接口
- 同名同义，但底层是 `ls_uart`
- 识别板 `BoardComm::init()` 会检查 `flush_buffer()`；运行板版本当前几乎总是返回 `true`

运行板命令行接口
- 图传：
  - `--stream`
  - `--no-stream`
  - `--stream=on|off`
  - `--stream-mode on|off`

识别板命令行接口
- 图传：
  - `--stream`
  - `--no-stream`
  - `--stream=on|off`
  - `--stream-mode on|off`
- 识别链：
  - `--recognition`
  - `--no-recognition`
  - `--recognition=on|off`
  - `--recognition-mode on|off`

键盘接口
- 运行板：
  - `c/C`
  - 位置：`running/Code/User/image/vision_runtime.cc / HandleManualVisionReset()`
  - 动作：`track_force_reset()`、`handler_sys.Stop_Action()`、`image_remote_recognition_reset()`
- 识别板：
  - `c/C`
  - 位置：`recognition/Code/User/src/recognition_runtime.cc / HandleManualRecognitionStart()`
  - 当 `BW_RECOG_REQUIRE_MANUAL_START != 0` 时，作用是启动一轮“红块检测 -> 识别 -> 发状态”

VOFA 接口
- 位置：
  - 头：`running/Code/User/inc/VOFA.h`
  - 实现：`running/Code/User/src/VOFA.cc`
- 当前发送字段顺序：
  - `target_L`
  - `speed_L`
  - `pwm_L`
  - `target_R`
  - `speed_R`
  - `pwm_R`
  - `yaw`
  - `base_speed`
  - `preview_curve_angle_deg`
  - `preview_img_y`
  - `big_langd_add`
- 当前已接入的远程指令：
  - `vrem:` -> `cfg_vofa_remote_enable`
  - `vadj:` -> `vofa_target_speed_adjust`

菜单接口
- 位置：
  - 头：`running/Code/User/inc/Menu.h`
  - 实现：`running/Code/User/src/Menu.cpp`
- 当前菜单直接写入这些变量：
  - `PID.base_target_speed`
  - `cfg_vofa_remote_enable`
  - `PID_Cube.Kp_a/Kp_b/Ki/Kd_a/Kd_b`
  - `PID_Speed_L.Kp/Ki/Kd`
  - `PID_Speed_R.Kp/Ki/Kd`
  - `esc_sys.enable_esc_diff`
  - `esc_sys.esc_diff_ratio`

**当前活跃链路与“存在但未接线”的逻辑**

这些点后续改动时最容易误判，必须以此处为准：

1. `WEAPON/SUPPLY` 当前不是 `TargetHandler` 纯角度绕行
- 活跃路径：`running/Code/User/image/image_data.cc` + `image_process.cc`
- 当前行为：左/右锁边，继续走正常 `MID -> path -> pure_angle` 链，并叠加一个短时激进 pure_angle 覆盖窗口
- 预留路径：`running/Code/User/src/TargetHandler.cc`
- 当前源码里没有任何地方在串口收包后调用 `handler_sys.Start_Action(...)`

2. `TargetHandler` 仍然被 `vision_runtime.cc` 轮询，但默认不会进入动作态
- `handler_sys.Update_Tick()` 每帧都会跑
- `TryGetPureAngleOverride()` 也每帧都会检查
- 但没有 `Start_Action()`，所以默认不接管

3. 运行板 `Mission.cc` 当前未接入主流程
- 文件在：`running/Code/User/inc/Mission.h` / `src/Mission.cc`
- 当前主入口、视觉链、控制链都没有使用它

4. TOF/路障状态机当前未接线
- 文件在：
  - `running/Code/User/inc/TOF.h`
  - `running/Code/User/inc/StateMachine.h`
  - `running/Code/User/src/TOF.cc`
  - `running/Code/User/src/StateMachine.cc`
- 但 `main.cpp` 里的 TOF 初始化与 `task_sensor_read()` 里的更新逻辑都被注释了

5. 活跃控制线程不是 `BayWatcher_Inner_Loop` / `BayWatcher_Outter_Loop`
- 当前启动的是：
  - `BayWatcher_Control_Loop`
  - `BayWatcher_Cube_Loop`
- 若改外环/内环逻辑，先确认你改的是实际启动的线程

6. 弯道减速代码存在，但当前活跃 `BayWatcher_Control_Loop` 没有启用
- `update_curve_slowdown_base_speed()` 的完整实现还在 `PID.cc`
- 但活跃主环里当前是：
  - `const float effective_base_speed = PID.base_target_speed;`
- 所以 `preview_curve_angle_deg` 当前不会在活跃主环里触发弯道减速

7. 斑马线检测链当前已经接入活跃控制线程
- 图像侧：
  - `image_process.cc` 里当前 owner 是“冲线锁定 + 斑马线消失后延迟停车”状态机
  - `BW_ZEBRA_RUSH_MODE=1` 表示单次冲线；`=2` 表示双次冲线
- 控制侧：
  - `handle_zebra_stop_request()` 已重新接回 `BayWatcher_Control_Loop()` 与 `BayWatcher_Cube_Loop()`
  - 冲线期间基础速度会额外乘上 `BW_ZEBRA_RUSH_SPEED_RATIO`

8. 两个 README 存在旧口径
- 运行板 README 还写着“远端识别状态适配层调用 `handler_sys.Start_Action(...)`”，当前源码不是这样
- 识别板 README 还写着“只发一次性事件包”，当前源码是“状态流 + 心跳补发”

**参数集中位置**

运行板 `running/Code/User/image/common.h`
- 图像基础参数：`IMAGE_H/W`、`PIXPERMETER`、`ROADWIDTH`
- 寻线与重采样：`SEARCH_LINE_START_OFFSET`、`RESAMPLEDIST`、`ANGLEDIST`
- `pure_angle` 预瞄与路径：`PUREANGLE_PREVIEW_*`、`PATH_BLEND_REF_IMAGE_Y`
- 正常道路 `x_right-x_left` 单调趋势退回 MIXED：`BW_NORMAL_FORCE_MIXED_*`
- 丢线/趋势前馈：`PUREANGLE_LOST_*`、`PUREANGLE_PRE_CTRL_*`
- 斑马线：`ZEBRA_*`
  - 冲线分支：`BW_ZEBRA_RUSH_MODE`、`BW_ZEBRA_RUSH_SPEED_RATIO`
- 双板识别联动：`BW_REMOTE_SIGN_AGGRESSIVE_*`、`BW_REMOTE_STATE_STALE_MS`、`BW_REMOTE_VEHICLE_HOLD_MS`、`BW_REMOTE_VEHICLE_SKIP_HALF_WIDTH`、`BW_REMOTE_U_SLOWDOWN_RATIO`
- 环岛单边偏移：`BW_CIRCLE_OFFSET_ENABLE`、`BW_CIRCLE_IN_OFFSET_RATIO`、`BW_CIRCLE_RUNNING_OFFSET_RATIO`
- 预瞄速度反馈：`PUREANGLE_PREVIEW_SPEED_FEEDBACK_ENABLE`
- `TargetHandler` 预留绕行参数：`BW_BYPASS_LEFT_*`、`BW_BYPASS_RIGHT_*`
- 图传：`BW_ENABLE_STREAM`、`BW_STREAM_LINE_USE_BINARY_VIEW`

运行板 `running/Code/User/src/PID.cc`
- 直道加速：
  - `cfg_straight_accel_enable`
  - `cfg_straight_accel_max_add`
  - `cfg_straight_accel_curve_*`
  - `cfg_straight_accel_yaw_*`
- 发车状态机：
  - `cfg_start_delay`
  - `cfg_esc_soft_start`
  - `cfg_motor_soft_start`
  - `g_startup_delay_ms`
  - `g_startup_esc_target`
  - `g_startup_esc_step`
  - `g_startup_speed_step`
- 弯道减速宏也在这里，但如上所述当前活跃主环未用

识别板 `recognition/Code/User/inc/common.h`
- 总开关：`BW_ENABLE_RECOGNITION`、`BW_ENABLE_STREAM`、`BW_RECOG_REQUIRE_MANUAL_START`
- 相机：`BW_RECOG_CAMERA_FPS`、`BW_RECOG_CAMERA_USE_MANUAL_EXPOSURE`、`BW_RECOG_CAMERA_MANUAL_EXPOSURE`
- 主循环节流：`BW_RECOG_LOOP_*`、`BW_RECOG_IDLE_SLEEP_MS`、`BW_RECOG_ACTIVE_SLEEP_MS`
- 日志与耗时：`BW_RECOG_ENABLE_PERF_LOG`、`BW_RECOG_TEXT_LOG_ENABLE`、`BW_RECOG_VERBOSE_LOG`
- 旧预筛残留宏：`BW_RECOG_NORMAL_PRECHECK_*`、`BW_RECOG_ULTRA_*`；当前 active flow 不再依赖它们
- ROI 与几何：`BW_RECOG_ROI_METHOD`、`BW_RECOG_TRIGGER_SEARCH_Y_*`、`BW_RECOG_RED_MASK_MAX_Y`、`BW_RECOG_WHITE_REFERENCE_ROW_Y`
  当前该参考行已用于白红带参考包络，不再是“只看白带”
- 分类决策阈值：`BW_RECOG_MIN_VALID_FRAMES`、`BW_RECOG_MAX_VALID_FRAMES`、`BW_RECOG_DECISION_TOP1_AVG_THRESHOLD`、`BW_RECOG_DECISION_MARGIN_THRESHOLD`
  当前 top1/margin owner 已回到 `common.h`，不再使用 `deploy_calibration.json` 覆盖
- 识别结果保持：`BW_RECOG_SIGN_LOSS_HOLD_MS`
- 图传：`BW_STREAM_PUBLISH_INTERVAL_FRAMES`、`BW_STREAM_MAX_WIDTH`、`BW_STREAM_JPEG_QUALITY`
- 串口心跳：`BW_RECOG_STATE_HEARTBEAT_INTERVAL_MS`
- 砖块阈值：`BW_RECOG_BRICK_MIN_AREA`

**常见改动定位索引**

| 想改什么 | 先看哪里 | 当前真实 owner/说明 |
| --- | --- | --- |
| 改双板串口帧格式、CRC、状态码枚举 | 两板 `Code/User/inc/Communication.h` 与 `src/Communication.cc` | 协议必须双边同步；识别板发、运行板收。 |
| 改识别板“何时发包/心跳周期” | `recognition/Code/User/src/recognition_runtime.cc` + `recognition/Code/User/inc/common.h` | `BW_RECOG_STATE_HEARTBEAT_INTERVAL_MS` 与 `tx_seq` 递增逻辑都在这里。 |
| 改识别类别到 `w/s/v` 的映射 | `recognition/Code/User/src/recognition_chain.cc` | 看 `vision_code_from_target_code()` 与最终 `parse_target_class_code(label)`。 |
| 改 `vehicle`/`u` 在运行板的保持航向/减速语义 | `running/Code/User/image/image_data.cc` + `common.h` + `running/Code/User/src/PID.cc` | `v/u` 都走 `VEHICLE_ROUTE` 锁存 `pure_angle`，但 `u` 还会通过 `image_remote_recognition_get_speed_ratio_override()` 下调基础速度。 |
| 改 `weapon/supply` 当前的左/右锁边行为 | `running/Code/User/image/image_data.cc` + `image_process.cc` | 当前主链不是 `TargetHandler`，而是 `FollowLine::MIDLEFT/MIDRIGHT`，并叠加 `BW_REMOTE_SIGN_AGGRESSIVE_*` 短时纯角接管。 |
| 把 `weapon/supply` 改成纯角度分阶段绕行 | `running/Code/User/src/TargetHandler.cc` + `running/Code/User/image/vision_runtime.cc` + 收包适配层 | 还需要新增 `handler_sys.Start_Action(...)` 的实际调用点。 |
| 改绕行每阶段角度/时长 | `running/Code/User/src/TargetHandler.cc` + `running/Code/User/image/common.h` | 时长在 `get_stage_duration_frames()`，角度在 `BW_BYPASS_LEFT_* / RIGHT_*`。 |
| 改灰度图预处理、二值图形态学、图传底图 | `running/Code/User/image/vision_runtime.cc` | 二值化和显示层都在这里。 |
| 改寻线、角点、单边处理、`pure_angle` 算法 | `running/Code/User/image/image_handle.cc` + `image_midline_process.cc` + `common.h` | `process_line()`、`MID()`、`BuildPathFromCoreToMidlineArc()`、`CalculatePureAngleFromPath()` 是主链核心。 |
| 改普通道路/坡道时“强制退回 MIXED”的 `x_right-x_left` 单调趋势判定 | `running/Code/User/image/image_midline_process.cc` + `running/Code/User/image/common.h` | 当前 owner 是 `MID()` 里 `should_force_mixed_by_width_trend(...)`，只在 `element_type==NORMAL && !zebra_stop` 下生效；命中后会置 `g_force_mixed_slope_active=true`，`vision_runtime.cc` 会叠字输出 `SLOPE`。 |
| 改元素判定、环岛、十字、远端线 | `running/Code/User/image/element.cc` + `element/circle.cc` + `element/crossing.cc` | 元素投票 owner 在 `element.cc`。 |
| 改斑马线判定或停车时序 | `running/Code/User/image/element/zebra.cc` + `image_process.cc` + `PID.cc` | 斑马线当前由 `image_process.cc` 管“单次/双次冲线、锁 MIXED、延迟停车”，由 `PID.cc` 管“冲线速度倍率 + 最终停车执行”。 |
| 改 `pure_angle` 到 `speed_adjust` 的映射 | `running/Code/User/src/PID.cc` | 当前活跃是 `BayWatcher_Cube_Loop()`。 |
| 改底盘速度环、差速、软启动、电调 | `running/Code/User/src/PID.cc` + `ESC.cc` + `Motor.cc` + `Encoder.cc` | 实际启动线程是 `Control_Loop` 与 `Cube_Loop`。 |
| 改菜单可调参数或菜单页面 | `running/Code/User/src/Menu.cpp` | 页面编号、指针绑定、读写变量都在这里。 |
| 改 VOFA 数据字段或上位机命令 | `running/Code/User/src/VOFA.cc` | 发包字段顺序和 `vrem:`/`vadj:` 解析都在这里。 |
| 改识别板主循环节流、空跑 CPU、活动 fps | `recognition/Code/User/inc/common.h` + `recognition_runtime.cc` | 先看 `BW_RECOG_LOOP_*`；旧 `BW_RECOG_NORMAL_PRECHECK_*` 当前不是 active flow owner。 |
| 改识别板 ROI 搜索带、白色参考行、砖块阈值、顶边扩展策略 | `recognition/Code/User/inc/common.h` + `roi_runtime_geometry.cc` | 搜索带和真实 ROI 几何在这里。 |
| 改识别板 ROI 低信息 reject | `recognition/Code/User/src/roi_runtime_geometry.cc` | `ComputeLowInformationRoiMetrics()` 是唯一 owner。 |
| 改 ONNX 模型、类名、温度/偏置校准 | `recognition/model/*` + `recognition/Code/User/src/recognition_chain.cc` | `Initialize()` 负责找模型、类名和 `deploy_calibration.json`。 |
| 改两板逆透视表 | 两板各自 `Code/User/image/transform_table.*` 或 `Code/User/inc/transform_table.*` | 运行板维度是 `120x160`，识别板维度是 `480x640`。 |
| 新增运行板源码文件 | `running/Code/**` 即可 | `running/CMakeLists.txt` 会自动收进构建。 |
| 新增识别板源码文件 | `recognition/Code/User/**` + `recognition/CMakeLists.txt` | 必须显式加入 `RECOGNITION_BOARD_SRC`。 |

**最后的判断顺序**
- 先判断改动属于哪一块：
  - “识别看到了什么” -> `recognition`
  - “车如何跟线/绕行/控车” -> `running`
- 再判断改动是：
  - 参数问题 -> 两个 `common.h` 或 `PID.cc`
  - 状态 owner 问题 -> `image_data.cc` / `recognition_chain.cc`
  - 算法问题 -> `image_handle.cc` / `image_midline_process.cc` / `roi_runtime_geometry.cc`
  - 协议问题 -> 两板 `Communication.*`
- 若碰到旧 README 与这里冲突，以源码和本摘要为准。
